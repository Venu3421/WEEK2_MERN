//only once per repository/folder
git init
// below commands must be run for each new change
git add . (stages the current folder)
git commit -m "commit msg"
//only once per user
git config --global user.email "venupaka006@gmail.com"
git config --global user.name "name"
